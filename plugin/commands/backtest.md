---
description: Run or design a leakage-aware strategy sanity test
argument-hint: "strategy + data/period + costs"
---

# Backtest

Use `strategy-testing-india`. State signal timing, execution assumption, costs, slippage, sample period, benchmark and validation method. Prefer a simple baseline before complex optimisation.
