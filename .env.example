# Level 1 needs no credentials.
# Optional override for cached raw NSE/BSE bhavcopy reports:
ITA_MARKET_DATA_CACHE=

# Future Level 2 provider adapters may introduce provider-specific credentials.
# Keep them in local environment/secrets only; never commit them.
